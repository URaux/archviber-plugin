{
  "name": "archviber-plugin",
  "version": "0.1.0",
  "type": "module",
  "description": "ArchViber — Architecture visualization plugin for Claude Code",
  "scripts": {
    "build": "tsc",
    "test": "node --import tsx core/__tests__/core.test.ts"
  },
  "devDependencies": {
    "tsx": "^4.0.0",
    "typescript": "^5.5.0"
  }
}
